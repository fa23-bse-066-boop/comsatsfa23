# libs/shared-ui

This library contains shared UI components, directives, pipes, and design tokens used by both the user and admin portals.

## Running unit tests

Run `nx test shared-ui` to execute the unit tests.
